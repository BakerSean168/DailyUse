# DailyUse 生产环境配置
# =============================================================================
# 此文件包含生产环境的默认值，可安全提交到 Git
# ⚠️ 敏感配置（密码、密钥）必须在 .env.production.local 或环境变量中设置
# =============================================================================

NODE_ENV=production
API_PORT=3000
API_HOST=0.0.0.0
LOG_LEVEL=info
TZ=Asia/Shanghai

# 数据库（生产环境需要在 .env.production.local 中设置完整的 DATABASE_URL）
# DATABASE_URL=postgresql://user:password@host:5432/dailyuse?schema=public

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# JWT（生产环境必须在 .env.production.local 中设置！）
# JWT_SECRET=your-production-secret-at-least-64-characters
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS（生产环境必须设置具体域名！）
# CORS_ORIGIN=https://yourdomain.com

# 功能开关
ENABLE_DAILY_ANALYSIS=true
USE_PRIORITY_QUEUE_SCHEDULER=true

# 文件上传
UPLOAD_MAX_SIZE=10485760
