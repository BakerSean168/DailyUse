{
  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 基础路径：相对路径的起点
    "baseUrl": ".",

    // 输出目录：编译后的 JavaScript 和类型声明文件
    "outDir": "./dist",

    // 源码根目录：限制编译范围
    "rootDir": "./src",

    // ============================================================
    // Vite 构建模式配置
    // ============================================================

    // 不使用 tsc 输出，由 vite 统一处理
    "noEmit": true,

    // 禁用增量编译（noEmit 时不能使用 incremental）
    "incremental": false,

    // ============================================================
    // 库环境配置（UI 组件库）
    // ============================================================

    // 标准库：包含 ES2020 和浏览器 DOM API
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    // JSX 支持：Vue 3 使用 preserve 模式
    "jsx": "preserve",

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]

      // 依赖的包通过 references 引用
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件：TypeScript 文件和 Vue 单文件组件
  "include": ["src/**/*", "src/**/*.vue"],

  // 排除的文件
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"]
}
