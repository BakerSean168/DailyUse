{
  "name": "infrastructure-client",
  "root": "packages/infrastructure-client",
  "sourceRoot": "packages/infrastructure-client/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:infrastructure", "platform:client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsup",
        "cwd": "packages/infrastructure-client"
      },
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/infrastructure-client"
      }
    }
  }
}
