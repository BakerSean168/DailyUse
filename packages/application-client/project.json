{
  "name": "application-client",
  "root": "packages/application-client",
  "sourceRoot": "packages/application-client/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:application", "platform:client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsup",
        "cwd": "packages/application-client"
      },
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/application-client"
      }
    }
  }
}
